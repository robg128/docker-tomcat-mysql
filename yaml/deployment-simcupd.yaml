apiVersion: apps/v1
kind: Deployment
metadata:
  name: simcupd-deployment
  namespace: simcupd-development
  labels:
    app: simcupd
    version: "1.0"
    environment: development
spec:
  replicas: 2
  selector:
    matchLabels:
      app: simcupd
      version: "1.0"
      environment: development
  template:
    metadata:
      labels:
        app: simcupd
        version: "1.0"
        environment: development
    spec:
      containers:
      - name: simcupd-container
        image: 673973395263.dkr.ecr.us-east-2.amazonaws.com/simcupd:1.0
        ports:
        - containerPort: 8080
